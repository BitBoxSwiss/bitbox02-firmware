#include "select_ctap_credential.h"

int workflow_select_ctap_credential(ctap_credential_display_list_t* credentials) {
    /* TODO */
    /* Just select the first credential in the list for now. */
    (void)credentials;
    return 0;
}
